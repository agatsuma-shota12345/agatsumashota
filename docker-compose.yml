services:
  # nginxコンテナ
  nginx:
    container_name: nginx
    build:
      context: .
      dockerfile: nginx/Dockerfile
    ports:
      - 80:80
    volumes:
      - nginx:/var/www

  volumes:
    nginx:
